# Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=control_bus
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/control_bus?schema=public

# Auth
JWT_SECRET=tu_secreto_super_seguro_aqui
JWT_EXPIRES_IN=24h

# Server
PORT=3000
NODE_ENV=development

# SMTP Global (Super Admin)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=user@example.com
SMTP_PASS=password
SMTP_FROM_NAME="Control Bus Alerts"
SMTP_FROM_EMAIL=alerts@example.com

# Traccar API (Optional if calling Traccar API back)
TRACCAR_URL=http://your-traccar-server:8082
TRACCAR_USER=admin
TRACCAR_PASS=admin
